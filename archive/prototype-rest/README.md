Archived Prototype REST Scaffolds
=================================

These files were retained only as experiments.

They are not the main path for this project because:

 * they do not implement NIST target-native submission interfaces
 * they are useful for matcher prototyping, not official compatibility work
 * keeping them on the main path would create ambiguity for Jupiter
